# Reports

Generated metrics and figures are written here by `scripts/train_baselines.py`.

The `figures/` folder and generated metric files are ignored by Git by default. Curate only final presentation-ready outputs when preparing a portfolio release.
